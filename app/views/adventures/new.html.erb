<%= form_for([@bucket, @adventure]) do |f| %>
   <% if @adventure.errors.any? %>
    
    <div id="error_explanation">
      <h2><%= pluralize(@adventure.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @adventure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  
  <% end %>

    <div>
      <%= f.label :adventure_description %>
      <%= f.text_field :adventure_description %>
    </div>

    <div>
      <%= f.submit 'Create a new item!' %>
      <%= link_to 'Cancel', user_bucket_lists_path(@bucket.user) %>
    </div>

<% end %>


