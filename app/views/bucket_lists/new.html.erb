<%= form_for @bucket_list do |f| %>
  <% if @bucket_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bucket_list.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @bucket_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>


  <%= f.submit 'Save List!' %>

<%- end -%>
